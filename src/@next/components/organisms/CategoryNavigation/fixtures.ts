import { IProps } from "./types";

const category: any = {
  __typename: "Category",
    ancestors: null,
    backgroundImage: null,
    children: null,
    id: "",
    name: "",
    seoDescription: null,
    seoTitle: null,
};

export const DEFAULT_PROPS: IProps = {  
  category,
};
